# Caddy Variables
PORT_FRONTEND=3000
PORT_BACKEND=8080
AUTO_HTTPS=on
CADDYFILE_PATH=

# Build Time Vars
NODE_ENV=development
GO_ENV=development
NEXT_PUBLIC_SERVER_URI=http://localhost:8080
NEXT_PUBLIC_WS_URL=ws://localhost:8080


# Server Configuration
PORT=8080
JWT_SECRET=secret
# COOKIE_DOMAIN leave blank for developemnt or use .domain.com
COOKIE_DOMAIN=
COOKIE_SECURE=true


# URLs and Endpoints
FRONTEND_URI=http://localhost:3000
SERVER_URI=http://backend:8080 (This is the internal dns for docker services commmunicating with other serivces specifically the backend)

# OAuth Settings
OAUTH_PROVIDER_NAME=google
OAUTH_CLIENT_ID=client-id
OAUTH_CLIENT_SECRET=client-secret
OAUTH_REDIRECT_URL=http://localhost:8080/api/auth/callback

# Database Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=chatapp
POSTGRES_SSLMODE=disable

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
